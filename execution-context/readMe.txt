1) Execution Context Works -> box created when js code executed with memory allocation(variable and function are get alloted key:value pair) and code runs (all code is runned line by line)
2) synchronous single threaded language
3) Global Execution Context
4) callstack
5) creation and deletion of Execution Context managed by callstack